{% set name = "torchfsm" %}
{% set version = "0.0.6" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/qiauil/torchfsm/releases/download/v{{ version }}/torchfsm-{{ version }}.tar.gz
  sha256: c7a26f6de605c62f02ca209835baefe1ab43aa01e5ec1edd4169242ad1b7a01d

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools
  run:
    - python >={{ python_min }}
    - ipython
    - matplotlib-base
    - numpy
    - pytorch
    - tqdm
    - vape4d

test:
  imports:
    - torchfsm
  commands:
    - pip check
  requires:
    - python {{ python_min }}
    - pip

about:
  home: https://qiauil.github.io/torchfsm
  summary: Fourier Spectral Method with PyTorch
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - sarthakpati
